<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<!--
  The container app runs outside the App Sandbox so it can maintain Quick Look
  on the user's behalf (reset the cache, prune stale registrations) via
  qlmanage/lsregister. It is a trivial onboarding launcher with no sensitive
  capability. The preview EXTENSION, which processes untrusted file content,
  stays sandboxed (see Sources/QuickLookExtension/Extension.entitlements).
-->
<dict>
</dict>
</plist>
